# Broken Page — Failing Spec

## Assertions

### DOM: `document.title` == `Broken Page`
### DOM: `document.querySelector('h1')` != `null`
### DOM: `document.querySelector('h1').textContent` == `Expected Title`
### Console: no errors
### Async: page loads without uncaught errors
### Timeout: 5000ms
